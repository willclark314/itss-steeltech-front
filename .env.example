# 复制为 .env.development.local 可覆盖本地配置（该文件已被 git 忽略）

# API 基础路径（开发/生产通常用 /api，由代理或 Nginx 转发）
VITE_API_BASE_URL=/api

# 开发阶段是否使用 Mock（true=脱离后端，false=联调真实后端）
VITE_USE_MOCK=true

# 关闭 Mock 时，Vite 将 /api 代理到此地址
VITE_API_PROXY_TARGET=http://localhost:5000
